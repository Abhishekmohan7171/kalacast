{
  "hosting": {
    "site": "kalacast-app",
    "public": "dist/kalacast/browser",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}

// ng build --configuration production
// firebase deploy --only hosting